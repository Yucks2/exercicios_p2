import java.util.Scanner;

public class Main
{
    public static void main(String[] args)
    {
        Scanner scanner = new Scanner(System.in);

        while()

        System.out.print("Numero da conta: ");
        int numero_conta = scanner.nextInt();
        scanner.nextLine();
        
        System.out.print("Nome do proprietario: ");
        String nome_proprietario = scanner.nextLine();

        System.out.print("Saldo total: ");
        double saldo_total = scanner.nextDouble();

        System.out.print("Limite de retirada: ");
        double limite_retirada = scanner.nextDouble();

        
    }
}